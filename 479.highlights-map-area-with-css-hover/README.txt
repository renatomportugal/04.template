A Pen created at CodePen.io. You can find this one at https://codepen.io/solipsistacp/pen/Cbdeg.

 Use CSS clip-path (SVG filter for compatibility). Pure CSS.<br>
===<<br>
<b>Destaca map / area con hover CSS.</b><br>

Usa clip-path CSS (con filtro SVG para compatibilidad). CSS puro.
<br>

Chrome, Firefox, Opera18.